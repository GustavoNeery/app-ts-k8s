name: CI

on:
  push:
    branches: 
      - main

permissions:
  contents: write
  id-token: write

jobs:
  build:
    name: 'Build, Push and Deployment'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: 'yarn'

      - name: Install dependencies
        run: yarn

      - name: Run tests
        run: yarn run test

      - name: Generate Tag
        id: generate_tag
        run: |
          SHA=$(echo $GITHUB_SHA | head -c7)
          echo "sha=$SHA" >> $GITHUB_OUTPUT

      - name: Build container image
        run: docker build -t ${{ vars.IMAGE_BASE_NAME }}:${{ steps.generate_tag.outputs.sha }} .

      - name: Login to ContainerRegistry
        uses: docker/login-action@v4
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      - name: Push image
        run: |
          docker push ${{ vars.IMAGE_BASE_NAME }}:${{ steps.generate_tag.outputs.sha }}
          docker tag ${{ vars.IMAGE_BASE_NAME }}:${{ steps.generate_tag.outputs.sha }} ${{ vars.IMAGE_BASE_NAME }}:latest
          docker push ${{ vars.IMAGE_BASE_NAME }}:latest


      # - name: Configure cloud provider credentials
      #   uses: aws-actions/configure-aws-credentials@v4
      #   with:
      #     role-to-assume: ${{ vars.AWS_ROLE_TO_ARN }}
      #     aws-region: ${{ vars.AWS_REGION }}
      
      # - name: Update kubeconfig
      #   run: aws eks update-kubeconfig --name ${{ vars.CLUSTER_NAME }} --region ${{ vars.AWS_REGION }}

      # - name: Deploy to K8S
      #   run: |
      #     sed -i "s|TAG_IMAGE|${{ steps.generate_tag.outputs.sha }}|g" k8s/deployment.yaml
      #     kubectl appy -f k8s/deployment.yaml -n app
      #     kubectl rollout status deployment/app-ts -n app
